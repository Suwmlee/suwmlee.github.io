---
layout: post
title: 部署命令备份
categories: [docker, jellyfin]
---


install package
```sh
docker run -d \
  --name=jellyfin \
  -e PUID=1000 \
  -e PGID=1000 \
  -e TZ=Asia/Shanghai \
  -p 8096:8096 \
  -v /path/to/library:/config \
  -v /path/to/tvseries:/data/tvshows \
  -v /path/to/movies:/data/movies \
  -e DOCKER_MODS=linuxserver/mods:universal-package-install
  -e INSTALL_PACKAGES=fonts-noto-cjk-extra
  --restart unless-stopped \
  linuxserver/jellyfin:latest
```


```sh
docker run -d \
  --name=thelounge-irc \
  -e PUID=0 \
  -e PGID=0 \
  -e TZ=Asia/Shanghai \
  -p 9000:9000 \
  -v /root/thelounge/config:/config \
  --restart unless-stopped \
  linuxserver/thelounge:latest
```


```

docker run --name kavita -d  -e TZ=Asia/Shanghai -p 5000:5000 -v "F:\docker\komga:/manga" -v "F:\docker\kavita:/kavita/config" --restart unless-stopped kizaing/kavita:latest

```

